set -ex

mkdir ${BOSH_INSTALL_TARGET}/bosh
cp runtime.env ${BOSH_INSTALL_TARGET}/bosh/runtime.env
cp compile.env ${BOSH_INSTALL_TARGET}/bosh/compile.env

cd ${BOSH_INSTALL_TARGET}
mkdir jre
tar zxvf ${BOSH_COMPILE_TARGET}/openjdk-*.tar.gz -C jre

# latest JRE release didn't have correct permissions
chmod -R a+r jre
